beforeEach(() => {
	console.log("before test");
});

afterEach(() => {
	console.log("after test");
});

describe("Endpoints", () => {
	it("GET /dev/test", async () => {
		console.log("get test");
	});
});